const viewer=document.querySelector('#viewer'),large=document.querySelector('#large');document.querySelectorAll('.postcard').forEach(button=>button.addEventListener('click',()=>{large.src=button.dataset.full;large.alt=button.getAttribute('aria-label');viewer.showModal()}));document.querySelector('#close').addEventListener('click',()=>viewer.close());viewer.addEventListener('click',event=>{if(event.target===viewer)viewer.close()});